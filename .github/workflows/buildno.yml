name: buildNumber

on:
  workflow_dispatch:
env:
  GITHUB_TOKEN: ${{ secrets.GHUB_TOKEN }}
jobs:
  buildNumber:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run : echo $BUILD_NUMBER
