name: DLT_STALE

on:
  workflow_dispatch:

jobs: 
  listStaleBranches:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          ref: ${{ github.head_ref }} 
          
      - name: List stale branches
        run: git branch 
