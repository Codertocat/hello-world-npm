name: STG_DEPLLOY
on:
  workflow_call:
  schedule:
    - cron: '*/5 * * * *'
jobs:        
   Deploy:
    runs-on: ubuntu-latest
    environment: STG
    steps: 
      - run: |
          echo stage deployment steps
      - name: disable workflow
        run:  |
          gh workflow disable STGdeploy.yml
        env:
              GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
